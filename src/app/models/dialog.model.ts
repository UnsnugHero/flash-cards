export interface ConfirmDialogData {
  dialogTitle: string;
  bodyMessage?: string;
  cancelButtonText?: string;
  confirmButtonText?: string;
  // maybe color for button? perhaps I want a red button for delete? but it may not always be a delete use
}
